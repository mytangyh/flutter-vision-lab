# Required OpenAI-compatible endpoint. Do not commit a real endpoint or key here.
VLM_BASE_URL=
VLM_API_KEY=
VLM_MODEL=doubao-seed-2-1-pro-260628
VLM_DISABLE_THINKING=true
VLM_IMAGE_DETAIL=low
VLM_MAX_TOKENS=160

# Required shared token for local research only; use at least 32 random characters.
# Replace this scheme with real authentication before production deployment.
CLIENT_TOKEN=
REQUEST_TIMEOUT_SECONDS=20
MAX_IMAGE_BYTES=2097152
MAX_IMAGE_DIMENSION=4096
